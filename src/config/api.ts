/**
 * API configuration.
 *
 * Appointment / callback requests POST to the shared callback REST API
 * (same endpoint the Center for Primary Care site uses). POST /callbacks
 * creates an appointment/callback record.
 */
export const API_CONFIG = {
  CALLBACK_API: 'https://st3ir5kcgl.execute-api.us-east-1.amazonaws.com/prod',
}
